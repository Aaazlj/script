# 复制成 .env 后填写（.env 已被 .gitignore 忽略，不要提交）
#
# Cloudflare Tunnel 连接器 token：
#   Cloudflare Dashboard → Zero Trust → Networks → Tunnels → 选中隧道 → 复制 token
# 或通过 API：GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/token
#
# 留空时 cloudflared 容器会起不来（面板本身不受影响）。
CF_TUNNEL_TOKEN=
